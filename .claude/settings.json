{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null); if [ \"$branch\" = \"main\" ] || [ \"$branch\" = \"master\" ]; then echo '{\"continue\": false, \"stopReason\": \"You are on the main branch. Please create a feature branch first (e.g. git checkout -b feat/my-feature).\"}'; fi",
            "statusMessage": "Checking git branch..."
          }
        ]
      }
    ]
  }
}
